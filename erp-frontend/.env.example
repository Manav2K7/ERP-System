# Optional. Default is "/api" which uses the Vite dev proxy (vite.config.js).
# Only set this if you point the SPA directly at a backend on another origin
# (that backend must then have CORS enabled).
# VITE_API_BASE_URL=http://localhost:8080/api
